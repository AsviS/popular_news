name: clean_news_ai
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: 0.1.2
  worker_manager: ^2.3.4
  osam: ^2.6.6
  flutter_sticky_header: ^0.4.0
  path_provider: ^1.5.1
  hive: ^1.3.0
  hive_flutter: ^0.3.0+1
  http: ^0.12.0+4
  intl: ^0.16.0
  share: ^0.6.3+5
  uuid: ^2.0.4
  after_layout: ^1.0.7+2

dev_dependencies:
  hive_generator:
    git:
      url: https://github.com/Renesanse/hive_generator.git
  build_runner:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
